import { Inter } from "next/font/google";

const sans = Inter({ subsets: ["latin"] });

export const siteConfig = {
  font: {
    sans,
  },
  description:
    "Church Finder PH: Find a church where you will be planted and grow",
};
